#include "main.h"

/**
 * add - adds two integers and returns
 * the result.
 * int add: Prototype function.
 * @a: number one.
 * @b: number two.
 * Return: Add of number one and number two.
 */
int add(int a, int b)
{
	return (a + b);
}
